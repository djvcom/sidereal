[package]
name = "hello-world"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
sidereal-sdk = { path = "../../crates/sidereal-sdk" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
